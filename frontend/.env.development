# ─── Local Development ─────────────────────────────────────────────────────
# This file is for LOCAL DEVELOPMENT only.
# For production (Render), set VITE_API_BASE_URL in the Render dashboard.
# Vite reads this at BUILD TIME, not at runtime.
VITE_API_BASE_URL=http://localhost:5000/api
